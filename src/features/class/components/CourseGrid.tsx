// // @/components/manager/academic/course/CourseGrid.tsx
// "use client";

// import { CourseSearchBar } from "./CoursesSearchBar";

// export function CourseGrid() {
//   // const { data, isLoading } = useCourses();
//   // const items = (data?.items ?? []) as Course[];

//   return (
//     <div>
//       {/* 헤더 + 추가 버튼 */}
//       <div className="flex items-center justify-between mb-4">
//         <button className="text-[11.5px] font-medium text-white bg-slate-800 px-3 py-1.5 rounded hover:bg-slate-700 transition-colors flex items-center gap-1">
//           + 추가
//         </button>
//       </div>
//       {/* 검색/필터 바 */}
//       <div className="border border-slate-200 rounded-lg p-4 bg-white mb-4">
//         <CourseSearchBar />
//       </div>
//       {/* 카드 그리드 */}
//       {/* {isLoading ? (
//         <p className="text-center text-[12px] text-slate-400 py-8">
//           불러오는 중...
//         </p>
//       ) : items.length === 0 ? (
//         <p className="text-center text-[12px] text-slate-400 py-8">
//           해당 조건의 강좌가 없습니다.
//         </p>
//       ) : (
//         <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
//           {items.map((course) => (
//             <CourseCard key={course.id} course={course} />
//           ))}
//         </div>
        
//       )} */}
//       ㅗ
//     </div>
//   );
// }
